digraph "antidote/src/clocksi_interactive_coord" { 
  rankdir=LR;
  edge [fontsize=10];
  node [shape=circle];
  ranksep = 2;
  nodesep = 0.5
  execute_op->receive_logging_responses[label="{OpType,Args}"]
  execute_op->committing[label="{OpType,Args}"]
  receive_prepared->terminate[label="timeout"]
  receive_committed->terminate[label="committed"]
  receive_prepared->receive_aborted[label="timeout"]
  committing->receive_committed[label="commit"]
  execute_op->start_tx[label="{OpType,Args}"]
  receive_read_objects_result->execute_op[label="{ok,{Key,Type,Snapshot}}"]
  receive_aborted->start_tx[label="ack_abort"]
  start_tx->execute_op[label="{start_tx,From,ClientClock,Properties,Operation}"]
  committing->terminate[label="commit"]
  init->execute_op[label=""]
  receive_read_objects_result->receive_read_objects_result[label="{ok,{Key,Type,Snapshot}}"]
  receive_logging_responses->single_committing[label="Response"]
  start_tx->execute_op[label="{start_tx,From,ClientClock,Properties}"]
  committing_2pc->receive_committed[label="commit"]
  committing_2pc->start_tx[label="commit"]
  receive_aborted->terminate[label="ack_abort"]
  receive_committed->receive_committed[label="committed"]
  execute_op->receive_read_objects_result[label="{OpType,Args}"]
  receive_prepared->receive_prepared[label="{prepared,ReceivedPrepareTime}"]
  receive_logging_responses->start_tx[label="Response"]
  receive_aborted->receive_aborted[label="_"]
  single_committing->start_tx[label="timeout"]
  committing_single->terminate[label="commit"]
  receive_logging_responses->receive_logging_responses[label="Response"]
  committing->start_tx[label="commit"]
  receive_prepared->start_tx[label="timeout"]
  init->execute_op[label=""]
  committing_single->start_tx[label="commit"]
  execute_op->receive_aborted[label="{OpType,Args}"]
  single_committing->terminate[label="timeout"]
  receive_logging_responses->execute_op[label="Response"]
  receive_logging_responses->committing[label="Response"]
  execute_op->receive_prepared[label="{OpType,Args}"]
  execute_op->terminate[label="{OpType,Args}"]
  receive_aborted->receive_aborted[label="ack_abort"]
  receive_logging_responses->terminate[label="Response"]
  receive_committed->start_tx[label="committed"]
  receive_logging_responses->receive_aborted[label="Response"]
  single_committing->start_tx[label="{committed,CommitTime}"]
  execute_op->execute_op[label="{OpType,Args}"]
  receive_prepared->receive_committed[label="{prepared,ReceivedPrepareTime}"]
  receive_prepared->committing[label="{prepared,ReceivedPrepareTime}"]
  receive_logging_responses->receive_prepared[label="Response"]
  execute_op->single_committing[label="{OpType,Args}"]
  committing_2pc->terminate[label="commit"]
  execute_op->committing_2pc[label="{OpType,Args}"]
  single_committing->committing_single[label="{committed,CommitTime}"]
  single_committing->receive_aborted[label="timeout"]
  receive_prepared->committing_2pc[label="{prepared,ReceivedPrepareTime}"]
  single_committing->terminate[label="{committed,CommitTime}"]
}